name: Chronos infrastructure failure control

on:
  workflow_dispatch:

jobs:
  unavailable-container:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/miguelalmeida0/chronos-retirement-infra-control:missing-20260813
    steps:
      - name: Must not execute
        run: echo container-initialization-unexpectedly-succeeded
